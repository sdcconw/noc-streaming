# 外部公開ポート（ホスト側）
MYSQL_PORT=3306
NOCSTREAM_PORT=3000
NOVNC_PORT=6080
VNC_HOST_PORT_RANGE="15900-15949"

# Docker Compose上のAPIコンテナからMySQLへ接続するURL
DATABASE_URL="mysql://noc_user:noc_pass@mysql:3306/noc_streaming"

# JWT署名鍵（必須）
JWT_SECRET="change-this-secret"

# 機密情報暗号化鍵（必須）
SECRET_ENCRYPTION_KEY="change-this-encryption-key"

# オフラインモード（trueでCDNではなくローカルBootstrap資産を使用）
OFFLINE=false

# Strict-Transport-Securityヘッダーの有効化
HSTS_ENABLED=true
HSTS_MAX_AGE=31536000
HSTS_INCLUDE_SUBDOMAINS=true

# CSPのupgrade-insecure-requests（trueでHTTP参照をHTTPSへ昇格）
CSP_UPGRADE_INSECURE_REQUESTS=false

# 認証トークン有効期限（秒）
AUTH_TOKEN_TTL_SECONDS=3600

# CookieにSecure属性を付与（HTTPS運用時はtrue）
COOKIE_SECURE=false

# 初期管理ユーザー
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="password"
ADMIN_FORCE_RESET_PASSWORD=true

# LDAP初期投入
LDAP_BOOTSTRAP_ENABLED=false
LDAP_SERVER_URL="ldaps://ldap.example.local:636"
LDAP_BIND_DN="cn=admin,dc=example,dc=local"
LDAP_BIND_PASSWORD="changeme"
LDAP_BASE_DN="dc=example,dc=local"
LDAP_USER_FILTER="(uid={username})"
LDAP_GROUP_DN="cn=noc-admins,ou=groups,dc=example,dc=local"

# VNC/noVNC
WORKER_VNC_ENABLED=true
VNC_PUBLIC_HOST="localhost"
VNC_BASE_PORT=15900
VNC_PORT_SPAN=50
VNC_TOKEN_MAX_ID=1000
NOVNC_ENABLED=true
NOVNC_PUBLIC_BASE_URL="http://localhost:6080"
